# Website

Website code has a separate lifecycle from the active portfolio research core.

- `website/src/` contains the former `src/website_*.py` helpers.
- `website/tests/` contains the former `tests/test_website_*.py` tests.
- Existing mockup and landing assets under `website/` remain in place.
